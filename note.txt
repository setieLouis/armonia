
# Docker builld image 
docker build -t fnm-node .

#Docker run 
 docker run -d \
  -v $(pwd):/workspace \
  -w /workspace \
  fnm-node \
  tail -f /dev/null

  docker exec -it charming_shirley /bin/bash
  npm install pdf-parse@1.1.1